# Optional. Leave blank to keep Course Journal Kit fully local-only.
# Set both to enable Supabase sync (cross-device backup + signed share links).
# Copy to `.env.local`, fill in, restart the dev server.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
